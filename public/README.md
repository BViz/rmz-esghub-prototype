This directory contains static assets that will be served directly at the root path (`/`).
For example, if you place `esg-framework.png` in this folder, you can reference it in your code as `<img src="/esg-framework.png" />`.
